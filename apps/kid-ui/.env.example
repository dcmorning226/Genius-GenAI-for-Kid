# Backend server host (your computer's LAN IP for mobile testing)
# Find your IP:
#   macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#   Windows: ipconfig | findstr "IPv4"
EXPO_PUBLIC_API_HOST=localhost
EXPO_PUBLIC_API_PORT=8000

# Production URLs (only used when __DEV__ is false)
# EXPO_PUBLIC_API_URL=https://api.companion.app
# EXPO_PUBLIC_WS_URL=wss://api.companion.app
